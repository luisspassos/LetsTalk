import { Base } from 'components/ConversationsPage/Sidebars/ConversationsListBase/FirstContent/AddContactButton';
import { Button } from './Button';

export function AddContactButton() {
  return <Base Buttons={Button} />;
}
